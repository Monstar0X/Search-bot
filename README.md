# Search-bot
A simple telegram bot. When u search for a channel it will give u the link to channel. The only channels it gives u is the ones I own. I have around 200 around channels some of them are private some of them are public links. Make the bot so it automaticall
